table! {
    amostras {
        id -> Nullable<Integer>,
        nome -> Varchar,
        matriz -> Varchar,
        dopante -> Varchar,
        autor -> Varchar,
        local -> Varchar,
    }
}/*
table! {
    logins {
        id -> Nullable<Integer>,
        email -> Varchar,
        password -> Varchar,
    }
}
table! {
    amostras {
        id -> Nullable<Integer>,
        usuario -> Varchar,
        senha -> Varchar,
        email -> Varchar,
        telefone -> Varchar,
    }
}*/